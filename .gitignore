# Python caches and local runtime files
__pycache__/
*.py[cod]
*.tmp

# Model weights and rolling checkpoints are intentionally kept off GitHub.
checkpoints/
*.pt
*.pth
*.ckpt
*.bin
*.safetensors

# Pickled model artifacts are excluded, except for the small experiment
# hyperparameter file explicitly tracked below.
*.pkl
!experiment1/best_params_VarCoNet_AICHA.pkl
!experiment2/best_params_VarCoNet_AICHA.pkl
!experiment2b/best_params_VarCoNet_AICHA.pkl
!experiment3/best_params_VarCoNet_AICHA.pkl

# Experiment 2B's local dataset cache is intentionally kept off GitHub.
experiment2b/dataset_cache/
# Experiment 3's local dataset cache is intentionally kept off GitHub.
experiment3/dataset_cache/
